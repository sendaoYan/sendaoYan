name: Metrics

on:
  schedule: [{cron: "0 0 * * *"}]   # 每天 UTC 0 点自动更新
  workflow_dispatch:                 # 支持手动运行
  push: {branches: ["main"]}         # 推送到 main 时运行

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          config_timezone: Asia/Shanghai
          # 常用插件：提交日历、语言统计
          # 注：achievements 插件当前有上游 bug（无法爬取 GitHub 搜索页总数），已禁用
          plugin_calendar: yes
          plugin_languages: yes
          plugin_languages_limit: 8
